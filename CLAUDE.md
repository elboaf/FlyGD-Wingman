# CLAUDE.md

The project guide lives in `AGENTS.md` and is imported below, so Claude Code
and every other agent front-end read the same copy. Do not add architecture
or convention text here; edit `AGENTS.md`. The one duplicate this file used
to be went stale by five packages and four thousand lines before anyone
noticed, which is why there is no longer a duplicate.

@AGENTS.md
