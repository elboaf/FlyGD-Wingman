# Approved correction execution

U-01/E-01 in the coordinator DECISIONS.md authorizes implementation on fixed base `ab06a6efde9ade0f40791d812d922356e8e6c16f`. Preserve the earlier implementation-notes.md and regression-output.txt as pre-fix evidence. Production ownership remains controller.py only.

1. Add deterministic tests for an unmeasured changed current identity, rename plus old-path reuse, detached folder-switch captures with/without a conflicting slot, retained uncached winner hydration, and the complete upload/log/URL/WorkGate path. Run them red before source edits.
2. Share exact-identity winner lookup across both admitted producers and accepted scan hydration. Snapshot weak retained objects briefly under link-store; never store a strong historical registry. First accepted definitive (including None) wins; no-verdict never becomes a cache winner or triggers an automatic retry.
3. Separate reconciliation's cache-dirty result and deferred row payloads. Producers save per drain/selection, independent of row-update acceptance. Current objects change only through RowSnapshot.set_duration; detached captures change directly. No WebView under link-store and no ffprobe under publication.
4. Cache slot admission checks installed identity even when unmeasured. With no current owner, preserve a conflicting slot. A newly accepted scan can hydrate an uncached retained RAM winner and persist it once that scan establishes slot ownership. Do not prune; current search finds durations.prune calls only in tests.
5. Run focused tests, own release-codec build/stage, full pytest with skips, Ruff, JS smoke and Cargo. Stop for unrelated gate/hook failures. Make an additive green candidate commit containing source/tests/concise notes only; keep bulky evidence local.
6. Run polish-core --fix over fixed-base..candidate, inspect changes, rerun fresh gates, add scoped corrective/evidence commits if needed. Return ordered SHAs and remaining native limits without push/integration.
