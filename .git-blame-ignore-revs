# Commits to skip when running `git blame`, so a mechanical reformat does
# not take credit for 136 files it only re-indented.
#
# Configure once per clone (this cannot be committed):
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# The hash below is only valid if this pull request is merged with a real
# merge commit. This repository squash-merges by default, and a squash
# would collapse the whole branch into one new commit, leaving no commit
# with this hash in main's history. Git ignores an unknown hash here
# SILENTLY -- no error, no warning -- and every reformatted file would
# stay attributed to the reformat regardless. If this branch was squashed
# by mistake, delete the line below rather than substituting the squash
# commit: that one also covers the lint fixes, the CI changes, and the
# noqa fix, none of which should be hidden from blame.

# Adopt ruff format across the tree (136 Python files, no behaviour change)
f20055e08ea4a74958702abe1a8329e3adc355fc
